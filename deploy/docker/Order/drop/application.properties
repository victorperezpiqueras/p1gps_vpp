# Parámetros de configuración por defecto de MongoDB.
# No hacen falta cuando se ejecute en local, pero en modo swarm(dockercompose)o en producción,
# hay que sobreescribir en Tomcat7 el archivo application.properties en el raíz.

mongodb.host: mongo
mongodb.database: ordering
